package Game.Player.User;

import Game.Player.Element.Element;
import Game.Player.Player;

public class Human extends Player {
    public Human(String name, Element element) {
        setName(name);
        setElement(element);
    }
}
